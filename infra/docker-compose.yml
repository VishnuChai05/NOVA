version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ohsou
      POSTGRES_USER: ohsou
      POSTGRES_PASSWORD: ohsou
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
