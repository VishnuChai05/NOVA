export default function SettingsPage() {
  return (
    <div>
      <h2>Settings</h2>
      <div className="card">
        <p>Configure scraper schedule and source queries in backend config files for v1.</p>
        <p>Planned in-app settings editor is tracked for next iteration.</p>
      </div>
    </div>
  );
}
